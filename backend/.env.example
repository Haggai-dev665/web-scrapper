# Server Configuration
PORT=8080
NODE_ENV=development

# Database Configuration
# For local development, use MongoDB running on localhost
# For production, use MongoDB Atlas connection string
MONGODB_URI=mongodb://localhost:27017/webscraper

# Example MongoDB Atlas URI (for production):
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/webscraper?retryWrites=true&w=majority

# Authentication
# Generate a secure random key for production:
# openssl rand -base64 32
JWT_SECRET=your-secret-key-here-change-in-production

# Puppeteer Configuration (for production/Heroku)
# PUPPETEER_SKIP_DOWNLOAD=true
# PUPPETEER_EXECUTABLE_PATH=/usr/bin/chromium-browser

# CORS Configuration
# ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# Rate Limiting
# RATE_LIMIT_WINDOW_MS=3600000
# RATE_LIMIT_MAX_REQUESTS=100
